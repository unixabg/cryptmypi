#!/bin/bash

# finalstuff hook call
if [ ! -z "${_ROOTPASSWD}" ]; then
	echo "Attempting encrypted pi root password change."
	chroot ${_BUILDDIR}/root /bin/bash -c "echo root:${_ROOTPASSWD} | /usr/sbin/chpasswd"
fi
